export class NotificationApp {
 public message: string;
  public sendMessageApp(total:number) {
    console.log(`Sending message on user account email, the transaction that will be made is s/.${total}`)
  }

}