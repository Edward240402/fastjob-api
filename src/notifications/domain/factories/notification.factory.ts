/*import { Notification } from '../entities/notification.entity';
import { NotificationId } from '../value-objects/notification-id.value';
import { IdContractor } from '../../../common/domain/value-objects/idContractor.value';
import { IdEmployee } from '../../../common/domain/value-objects/idEmployee.value';

export class NotificationFactory {
  public static createFrom(idContractor: IdContractor, idEmployee: IdEmployee): Notification {
    return new Notification(NotificationId.create(0), idContractor, idEmployee);
  }

  public static withId(notificationId: NotificationId, idContractor: IdContractor, idEmployee: IdEmployee): Notification {
    return new Notification(notificationId, idContractor, idEmployee);
  }
}*/

